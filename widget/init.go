// Code generated by javagen. DO NOT EDIT.

package widget

import (
	"fmt"
	"sync"
	"unsafe"

	"github.com/AndroidGoLab/jni"
)

// Ensure imports are used.
var (
	_ = fmt.Errorf
	_ = unsafe.Pointer(nil)
)

// jniTrue is the JNI representation of a true boolean value.
const jniTrue uint8 = 1

var (
	initOnce sync.Once
	initErr  error

	clsFrameLayout                                     *jni.GlobalRef
	midFrameLayoutGenerateLayoutParams1                jni.MethodID
	midFrameLayoutGetAccessibilityClassName            jni.MethodID
	midFrameLayoutGetConsiderGoneChildrenWhenMeasuring jni.MethodID
	midFrameLayoutGetMeasureAllChildren                jni.MethodID
	midFrameLayoutSetForegroundGravity                 jni.MethodID
	midFrameLayoutSetMeasureAllChildren                jni.MethodID
	midFrameLayoutShouldDelayChildPressedState         jni.MethodID
	midFrameLayoutGenerateLayoutParams1_1              jni.MethodID
)

func ensureInit(env *jni.Env) error {
	initOnce.Do(func() {
		initErr = doInit(env)
	})
	return initErr
}

// Init resolves all JNI class and method references for this package.
// It is safe to call multiple times (uses sync.Once internally).
// Exported for E2E testing on Android.
func Init(env *jni.Env) error {
	return ensureInit(env)
}

func doInit(env *jni.Env) error {
	var c *jni.Class
	var err error

	c, err = env.FindClass("android/widget/FrameLayout")
	if err != nil {
		return fmt.Errorf("find class android.widget.FrameLayout: %w", err)
	}
	clsFrameLayout = env.NewGlobalRef(&c.Object)

	midFrameLayoutGenerateLayoutParams1, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "generateLayoutParams", "(Landroid/util/AttributeSet;)Landroid/widget/FrameLayout$LayoutParams;")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutGetAccessibilityClassName, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "getAccessibilityClassName", "()Ljava/lang/CharSequence;")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutGetConsiderGoneChildrenWhenMeasuring, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "getConsiderGoneChildrenWhenMeasuring", "()Z")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutGetMeasureAllChildren, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "getMeasureAllChildren", "()Z")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutSetForegroundGravity, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "setForegroundGravity", "(I)V")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutSetMeasureAllChildren, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "setMeasureAllChildren", "(Z)V")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutShouldDelayChildPressedState, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "shouldDelayChildPressedState", "()Z")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	midFrameLayoutGenerateLayoutParams1_1, err = env.GetMethodID((*jni.Class)(unsafe.Pointer(clsFrameLayout)), "generateLayoutParams", "(Landroid/util/AttributeSet;)Landroid/view/ViewGroup$LayoutParams;")
	if err != nil {
		// Method may not exist on this device's API level; skip and
		// report at invocation time instead of failing the entire init.
		env.ExceptionClear()
	}

	return nil
}
